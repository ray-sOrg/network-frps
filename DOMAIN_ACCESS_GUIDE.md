# 🌐 域名访问配置指南

## 🎯 访问方式说明

### 1. **仪表板访问（推荐域名方式）**

- **域名**: `http://frps.ray321.cn`
- **说明**: 通过 Ingress 访问，无需端口号
- **服务**: FRPS Web 管理界面
- **状态**: ✅ 已配置，可通过域名访问

### 2. **FRP 服务访问（仅限 IP+端口）**

- **IP+端口**: `http://121.4.118.156:30000`
- **说明**: TCP 服务，无法通过 Ingress 访问
- **服务**: FRP 客户端连接端口
- **状态**: ⚠️ 必须使用 IP+端口，无法通过域名访问

## 🔍 为什么 FRP 服务不能用域名？

### 技术原因：

1. **FRP 服务是 TCP 服务**，不是 HTTP/HTTPS 服务
2. **Ingress 只能处理 HTTP/HTTPS 流量**
3. **TCP 服务需要通过 NodePort 或 LoadBalancer 暴露**

### 解决方案对比：

| 访问方式    | FRP 服务  | 仪表板  | 说明                 |
| ----------- | --------- | ------- | -------------------- |
| **域名**    | ❌ 不支持 | ✅ 支持 | 仪表板可通过域名访问 |
| **IP+端口** | ✅ 支持   | ✅ 支持 | 两种服务都支持       |

## 🛠️ 配置步骤

### 步骤 1: 确保域名解析正确

```bash
# 检查域名解析
nslookup frps.ray321.cn

# 应该返回你的服务器IP
# 121.4.118.156
```

### 步骤 2: 检查 Ingress Controller

确保你的 K3s 集群安装了 Ingress Controller：

```bash
# 检查Ingress Controller
kubectl get pods -n kube-system | grep ingress

# 如果没有，需要安装
kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.8.2/deploy/static/provider/baremetal/deploy.yaml
```

### 步骤 3: 验证 Ingress 状态

```bash
# 检查Ingress状态
kubectl get ingress -n frps

# 检查Ingress详细信息
kubectl describe ingress frps-dashboard-ingress -n frps
```

## 🌟 最佳实践建议

### 1. **仪表板访问**

- 使用域名：`http://frps.ray321.cn`
- 无需端口号，更专业
- 支持 HTTPS（如果配置了 SSL 证书）

### 2. **FRP 客户端配置**

- 在 frpc.ini 中使用 IP+端口：

```ini
[common]
server_addr = 121.4.118.156
server_port = 30000  # 使用NodePort端口
token = your_token
```

### 3. **域名管理**

- 主域名：`frps.ray321.cn` → 仪表板
- 子域名：`api.frps.ray321.cn` → 可考虑用于 API 服务

## 🔧 故障排查

### 问题 1: 域名 404 错误

**可能原因**:

1. Ingress Controller 未安装
2. Ingress 配置错误
3. 域名解析问题

**排查步骤**:

```bash
# 1. 检查Ingress Controller
kubectl get pods -n kube-system | grep ingress

# 2. 检查Ingress状态
kubectl get ingress -n frps

# 3. 检查域名解析
nslookup frps.ray321.cn

# 4. 检查Ingress日志
kubectl logs -n kube-system deployment/ingress-nginx-controller
```

### 问题 2: 仪表板无法访问

**可能原因**:

1. Pod 未运行
2. Service 配置错误
3. Ingress 规则不匹配

**排查步骤**:

```bash
# 1. 检查Pod状态
kubectl get pods -n frps

# 2. 检查Service状态
kubectl get svc -n frps

# 3. 检查Ingress规则
kubectl describe ingress -n frps
```

## 📋 配置检查清单

### ✅ 域名配置：

- [ ] 域名已解析到服务器 IP
- [ ] Ingress Controller 已安装
- [ ] Ingress 规则配置正确
- [ ] 服务端口映射正确

### ✅ 访问测试：

- [ ] 仪表板可通过域名访问
- [ ] FRP 服务可通过 IP+端口访问
- [ ] 所有服务正常运行

## 🎉 预期结果

配置完成后：

- ✅ **仪表板**: `http://frps.ray321.cn` (域名访问)
- ✅ **FRP 服务**: `http://121.4.118.156:30000` (IP+端口访问)
- ✅ **无端口号**: 仪表板通过域名访问，更专业
- ✅ **功能完整**: 两种访问方式都正常工作

---

**现在推送代码更新 Ingress 配置，然后测试域名访问！** 🚀
